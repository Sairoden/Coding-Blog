<h1 align="center"> <%= "Category: #{@category.name}" %> </h1>
<div class="mt-4">
  <%= will_paginate @category_articles, list_classes: %w(pagination justify-content-center)%>
</div>

<%= render "articles/article", obj: @category_articles %>
<div class="mt-4">
  <%= will_paginate @category_articles, list_classes: %w(pagination justify-content-center)%>
</div>